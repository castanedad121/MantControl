import React from "react";
import { Header, CtaSection, Footer } from "../components/index";

const Landing = () => {
  return (
    <div>
      <Header />
      <section id="about">
        <CtaSection />
      </section>

      <Footer />
    </div>
  );
};

export default Landing;
